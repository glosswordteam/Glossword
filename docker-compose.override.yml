services:
  php:
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.glossword.rule=Host(`glossword.local`)"
      - "traefik.http.routers.glossword.entrypoints=web"
      - "traefik.http.services.glossword.loadbalancer.server.port=80"
    networks:
      - web

networks:
  web:
    external: true